{
  "$schema": "http://json-schema.org/schema",
  "id": "Nx TestCafe Project Schematics Schema",
  "title": "Create TestCafe Configuration for the workspace",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    }
  },
  "required": []
}
