{
  "title": "TestCafe Target",
  "description": "TestCafe target option for Build Facade",
  "type": "object",
  "properties": {
    "devServerTarget": {
      "type": "string",
      "description": "Dev server target to run tests against."
    },
    "baseUrl": {
      "type": "string",
      "description":
        "Use this to pass directly the address of your distant server address with the port running your application"
    },
    "browsers": {
      "type": "array",
      "description": "The browser to run tests in.",
      "items": {
        "type": "string",
        "enum": ["firefox", "chrome"]
      }
    }
  },
  "additionalProperties": false,
  "required": []
}
